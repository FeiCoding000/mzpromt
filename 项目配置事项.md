# 项目配置事项

## Neon + Drizzle ORM 配置记录

本项目使用：

```txt
Next.js + Neon Serverless Postgres + Drizzle ORM
```

---

## 1. Neon 是什么

Neon 是 Serverless Postgres。

可以理解为：

```txt
Neon = 云端 PostgreSQL 数据库 + Serverless 扩缩容
```

它本质上还是 PostgreSQL，所以可以使用 SQL、Drizzle、Prisma 等工具连接。

---

## 2. 数据库 schema 选择

业务表建议放在默认的：

```txt
public
```

例如：

```txt
public.services
public.users
public.prompts
public.orders
```

注意：`public` 只是 PostgreSQL 默认 schema 名字，不代表所有人都可以公开修改数据。

权限控制应该通过：

```txt
后端代码权限判断
Admin 登录状态
数据库权限
RLS / Policy
```

`auth` schema 通常留给认证系统内部使用，不建议放普通业务表。

---

## 3. 已安装依赖

安装 Drizzle 和 Neon 连接库：

```bash
npm install drizzle-orm @neondatabase/serverless
```

安装 Drizzle CLI：

```bash
npm install -D drizzle-kit
```

安装 dotenv：

```bash
npm install -D dotenv
```

`dotenv` 主要用于让 `drizzle-kit` 读取本地 `.env.local`。

---

## 4. 环境变量

根目录创建：

```txt
.env.local
```

内容示例：

```env
DATABASE_URL="postgresql://USER:PASSWORD@HOST.neon.tech/DB_NAME?sslmode=require"
```

Neon 的连接字符串可以在 Neon 控制台复制。

项目中也提供了示例文件：

```txt
.env.example
```

注意：

```txt
.env.local 不要提交到 Git
```

---

## 5. Drizzle schema

数据库表结构写在：

```txt
db/schema.ts
```

当前示例表：

```ts
export const services = pgTable("services", {
  id: serial("id").primaryKey(),
  title: varchar("title", { length: 120 }).notNull(),
  slug: varchar("slug", { length: 120 }).notNull().unique(),
  description: text("description").notNull(),
  icon: varchar("icon", { length: 80 }),
  imageUrl: text("image_url"),
  isActive: boolean("is_active").default(true).notNull(),
  createdAt: timestamp("created_at").defaultNow().notNull(),
  updatedAt: timestamp("updated_at").defaultNow().notNull(),
});
```

Drizzle 可以从 schema 推导 TypeScript 类型：

```ts
export type Service = typeof services.$inferSelect;
export type NewService = typeof services.$inferInsert;
```

---

## 6. 数据库连接文件

数据库连接写在：

```txt
lib/db.ts
```

核心逻辑：

```ts
const sql = neon(process.env.DATABASE_URL);
export const db = drizzle(sql, { schema });
```

注意：数据库连接只能在服务端使用，不要在 Client Component 中直接导入 `db`。

不要这样做：

```tsx
"use client";
import { db } from "@/lib/db";
```

推荐在这些地方使用数据库：

```txt
Server Component
Server Action
Route Handler
服务端工具函数
```

---

## 7. Drizzle 配置文件

配置文件位于：

```txt
drizzle.config.ts
```

当前配置会读取 `.env.local`：

```ts
import { config } from "dotenv";
import { defineConfig } from "drizzle-kit";

config({ path: ".env.local" });

export default defineConfig({
  schema: "./db/schema.ts",
  out: "./db/migrations",
  dialect: "postgresql",
  dbCredentials: {
    url: process.env.DATABASE_URL!,
  },
});
```

为什么需要 dotenv？

Next.js 自己运行时会自动读取 `.env.local`，但 `drizzle-kit` 是独立 CLI，不是 Next.js，所以需要 `dotenv` 帮它读取本地环境变量。

---

## 8. package.json 脚本

当前已添加：

```json
{
  "db:generate": "drizzle-kit generate",
  "db:migrate": "drizzle-kit migrate",
  "db:push": "drizzle-kit push",
  "db:studio": "drizzle-kit studio"
}
```

用途：

```txt
npm run db:generate  根据 schema 生成 migration 文件
npm run db:migrate   执行 migration 到数据库
npm run db:push      开发阶段直接同步 schema 到数据库
npm run db:studio    打开 Drizzle Studio 查看数据库
```

---

## 9. 开发阶段常用流程

### 第一次配置

1. 在 Neon 创建数据库
2. 复制 connection string
3. 创建 `.env.local`
4. 填入：

```env
DATABASE_URL="postgresql://..."
```

5. 同步表结构：

```bash
npm run db:push
```

---

## 10. 修改表结构流程

修改：

```txt
db/schema.ts
```

开发阶段可以直接：

```bash
npm run db:push
```

更正式的 migration 流程：

```bash
npm run db:generate
npm run db:migrate
```

---

## 11. 基本查询示例

```ts
import { db } from "@/lib/db";
import { services } from "@/db/schema";
import { eq } from "drizzle-orm";

const data = await db
  .select()
  .from(services)
  .where(eq(services.isActive, true));
```

新增：

```ts
await db.insert(services).values({
  title: "Tax Services",
  slug: "tax-services",
  description: "Professional tax services",
});
```

修改：

```ts
await db
  .update(services)
  .set({ title: "New Title" })
  .where(eq(services.id, 1));
```

删除：

```ts
await db
  .delete(services)
  .where(eq(services.id, 1));
```

---

## 12. 生产环境注意事项

部署到 Vercel 后，不依赖 `.env.local` 文件。

需要在 Vercel 项目的 Environment Variables 里添加：

```txt
DATABASE_URL
```

Next.js 服务端会通过：

```ts
process.env.DATABASE_URL
```

读取生产环境变量。

---

## 13. 权限注意事项

即使业务表放在 `public` schema，也不代表用户可以随便修改。

普通用户和 admin 的权限应该在服务端控制。

例如 admin 修改数据前需要判断：

```ts
if (!user || user.role !== "admin") {
  throw new Error("Unauthorized");
}
```

前端不要直接连接数据库，所有数据库写入都应通过服务端完成。
